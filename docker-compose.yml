services:
  backend:
    build: .
    environment:
      - BRAVE_SEARCH_API_KEY=${BRAVE_SEARCH_API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - ADMIN_API_KEY=${ADMIN_API_KEY:-}
    volumes:
      - db-data:/app/data
    ports:
      - "8080:8080"

volumes:
  db-data:
